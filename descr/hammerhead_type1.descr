parms
    wc += gu;
    chk_both_strs = 0;

descr
    h5(seq = "G$", minlen = 1, tag = 'HI')

        ss(seq = "^N\{0,2\}[CT][TC]GAN[GT][AT]$", tag = 'Ubox')

            h5(minlen = 1, tag = 'HII')

                ss(minlen = 1, maxlen = 200, tag = 'HII_loop')

            h3(tag = 'HII')

        ss(seq = "^G[AG]", minlen = 3, maxlen = 4, tag = 'Pbox')

            h5(minlen = 1, tag = 'HIII')

                ss(minlen = 1, maxlen = 200, tag = 'HIII_loop')

            h3(seq = "T$", tag = 'HIII')

        ss(seq = "^A$", tag = 'cs')

    h3(tag = 'HI')
